/**
 * firmware.ino
 */
#include <Arduino.h>
#include "phoenix_globals.h"
#include "phoenix_pins.h"

void setup() {
  Serial.begin(115200);
  Serial.println(F("Serial initialized..."));
}

void loop() {
  
}